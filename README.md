# 📦 Telegram Product Bot

Бот для создания и продажи карточек товаров через Telegram с системой балансов, модерацией и админ-панелью.

[![Python 3.11+](https://img.shields.io/badge/python-3.11+-blue.svg)](https://www.python.org/downloads/)
[![Aiogram 3.x](https://img.shields.io/badge/aiogram-3.x-green.svg)](https://docs.aiogram.dev/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15+-blue.svg)](https://www.postgresql.org/)
[![Docker](https://img.shields.io/badge/Docker-✓-blue.svg)](https://www.docker.com/)
[![Code style: black](https://img.shields.io/badge/code%20style-black-000000.svg)](https://github.com/psf/black)

## ✨ Возможности

- 📦 **Создание карточек товаров** с названием, описанием, ценой и фото
- 👀 **Просмотр карточек** с инлайн-навигацией (вперед/назад)
- 💰 **Встроенная платежная система** на инвойсах
- 🏦 **Баланс пользователей** с функцией вывода средств
- 👨‍💼 **Админ-панель** с модерацией карточек
- 📊 **Статистика** по пользователям и карточкам
- 🔐 **Ролевая модель** (пользователи/администраторы)
- 📝 **Логирование** всех основных действий
- 🐳 **Docker-контейнеризация** для легкого развертывания

## 🚀 Быстрый старт

### Предварительные требования
- Python 3.11+
- PostgreSQL 15+
- Docker и Docker Compose (опционально)
- Токен Telegram бота от [@BotFather](https://t.me/BotFather)

### Установка и запуск

#### Способ 1: С Docker (рекомендуется)
```bash
# 1. Клонирование репозитория
git clone https://github.com/Hydra52Legit/TestTask---product-bot-in-Telegram
cd telegram-product-bot

# 2. Настройка переменных окружения
cp .env.example .env
# Отредактируйте .env файл, добавив ваш BOT_TOKEN и ADMIN_IDS

# 3. Запуск через Docker Compose
docker-compose up -d

# 4. Проверка работы
docker-compose logs -f bot
```

#### Способ 2: Без Docker
```bash
# 1. Клонирование репозитория
git clone https://github.com/ваш-username/telegram-product-bot.git
cd telegram-product-bot

# 2. Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или venv\Scripts\activate  # Windows

# 3. Установка зависимостей
pip install -r requirements.txt

# 4. Настройка базы данных PostgreSQL
# Создайте базу данных и пользователя:
# CREATE DATABASE product_bot;
# CREATE USER product_user WITH PASSWORD 'your_password';
# GRANT ALL PRIVILEGES ON DATABASE product_bot TO product_user;

# 5. Настройка переменных окружения
cp .env.example .env
# Отредактируйте .env файл

# 6. Запуск бота
python -m src.bot
```

## ⚙️ Конфигурация

### Файл .env
```env
BOT_TOKEN=ваш_токен_бота_от_BotFather
ADMIN_IDS=123456789,987654321  # ID администраторов через запятую
DB_HOST=localhost
DB_PORT=5432
DB_NAME=product_bot
DB_USER=postgres
DB_PASSWORD=postgres
LOG_LEVEL=INFO
```

### Получение Telegram ID
1. Откройте Telegram
2. Найдите бота [@userinfobot](https://t.me/userinfobot)
3. Отправьте команду `/start`
4. Бот покажет ваш ID

### Назначение администратора
Добавьте ID пользователя в переменную `ADMIN_IDS` в файле `.env`:
```env
ADMIN_IDS=123456789,987654321
```

## 🎯 Использование бота

### Для пользователей
- **/start** - Начать работу с ботом
- **📦 Добавить карточку** - Создать новую карточку товара
- **👀 Посмотреть карточки** - Просмотр доступных товаров
- **💰 Баланс** - Проверить баланс и вывести средства

### Для администраторов
- **👨‍💼 Админ меню** - Доступ к админ-панели
- **Модерация** - Просмотр и одобрение карточек
- **Статистика** - Статистика пользователей и карточек
- **Заявки на вывод** - Обработка заявок на вывод средств

## 🏗️ Архитектура проекта

```
telegram-product-bot/
├── docker-compose.yml     # Docker Compose конфигурация
├── Dockerfile            # Docker образ бота
├── requirements.txt      # Зависимости Python
├── .env.example         # Пример переменных окружения
├── src/                 # Исходный код
│   ├── bot.py           # Точка входа
│   ├── config.py        # Конфигурация приложения
│   ├── database/        # Модели и сессии БД
│   ├── handlers/        # Обработчики сообщений
│   ├── keyboards/       # Клавиатуры и кнопки
│   ├── middlewares/     # Middleware
│   ├── filters/         # Фильтры для хендлеров
│   ├── services/        Бизнес-логика
│   └── utils/           # Вспомогательные утилиты
├── tests/               # Тесты
└── alembic/             # Миграции базы данных
```

### Ключевые компоненты

1. **Handlers** - Обработчики команд и сообщений
2. **Services** - Бизнес-логика приложения
3. **Keyboards** - Клавиатуры и кнопки
4. **Middlewares** - Промежуточное ПО для БД и пользователей
5. **Filters** - Фильтры для админов и пользователей

## 🐳 Docker развертывание

### Сборка и запуск
```bash
# Сборка образов
docker-compose build

# Запуск в фоновом режиме
docker-compose up -d

# Просмотр логов
docker-compose logs -f bot
docker-compose logs -f postgres

# Остановка
docker-compose down

# Остановка с удалением данных
docker-compose down -v
```

### Команды для разработки
```bash
# Вход в контейнер бота
docker-compose exec bot bash

# Подключение к PostgreSQL
docker-compose exec postgres psql -U postgres -d product_bot

# Перезапуск только бота
docker-compose restart bot
```

## 🧪 Тестирование

```bash
# Установка зависимостей для тестирования
pip install pytest pytest-asyncio pytest-cov

# Запуск всех тестов
pytest

# Запуск с покрытием кода
pytest --cov=src --cov-report=html

# Запуск конкретного модуля тестов
pytest tests/test_handlers/

# Запуск с подробным выводом
pytest -v
```

### Структура тестов
```
tests/
├── conftest.py          # Фикстуры pytest
├── test_handlers/       # Тесты обработчиков
│   ├── test_common_handlers.py
│   ├── test_card_handlers.py
│   └── test_admin_handlers.py
└── test_services/       # Тесты сервисов
    ├── test_card_service.py
    └── test_payment_service.py
```

## 🔧 Разработка

### Настройка Git Flow
```bash
# Инициализация Git Flow
git flow init

# Создание фичи
git flow feature start feature-name

# Завершение фичи
git flow feature finish feature-name

# Создание релиза
git flow release start 1.0.0
git flow release finish 1.0.0
```

### Миграции базы данных
```bash
# Создание новой миграции
alembic revision --autogenerate -m "Описание изменений"

# Применение миграций
alembic upgrade head

# Откат миграции
alembic downgrade -1

# Просмотр текущей версии
alembic current
```

### Логирование
Логи сохраняются в папке `logs/`:
- `bot.log` - Основные логи приложения
- Автоматическая ротация при достижении 10MB
- Сохранение до 5 архивных файлов

## 📊 Админ-функции

### Модерация карточек
1. Просмотр карточек ожидающих модерации
2. Одобрение или отклонение карточек
3. Редактирование атрибутов карточек

### Статистика
- Количество пользователей
- Количество созданных карточек
- Статистика по каждому пользователю
- Графики и отчеты (в разработке)

### Заявки на вывод
1. Просмотр всех заявок на вывод
2. Подтверждение выплат
3. История всех транзакций

## 💰 Платежная система

### Как это работает
1. Пользователь выбирает товар для покупки
2. Бот создает инвойс (счет) на оплату
3. Пользователь оплачивает через Telegram Payments
4. Баланс продавца увеличивается на сумму покупки
5. Продавец может вывести средства на свои реквизиты

### Комиссии
- Комиссия за пополнение: 0%
- Комиссия за вывод: 5% (настраивается в конфиге)
- Минимальная сумма вывода: 100 рублей

## 🚨 Устранение неполадок

### Распространенные проблемы

**Проблема:** Бот не запускается
```bash
# Проверьте токен бота
echo $BOT_TOKEN

# Проверьте подключение к БД
docker-compose exec postgres pg_isready
```

**Проблема:** Ошибка подключения к PostgreSQL
```bash
# Проверьте статус PostgreSQL
docker-compose ps postgres

# Проверьте логи
docker-compose logs postgres

# Перезапустите контейнер
docker-compose restart postgres
```

**Проблема:** Ошибки импорта Python
```bash
# Переустановите зависимости
pip uninstall -r requirements.txt -y
pip install -r requirements.txt

# Проверьте структуру проекта
python -c "import sys; print(sys.path)"
```

### Логирование и отладка
```python
# Включение подробного логирования
LOG_LEVEL=DEBUG

# Просмотр логов в реальном времени
docker-compose logs -f --tail=100 bot
```

## 🔐 Безопасность

### Рекомендации по безопасности
1. **Никогда не коммитьте `.env` файл**
2. Используйте разные пароли для продакшн и разработки
3. Регулярно обновляйте зависимости
4. Используйте HTTPS для продакшн-окружения
5. Ограничьте доступ к админ-панели по IP

### Переменные окружения для продакшн
```env
# Продакшн настройки
DB_PASSWORD=strong_password_here
ADMIN_IDS=restricted_admin_ids
LOG_LEVEL=WARNING
ENVIRONMENT=production
```

## 📈 Масштабирование

### Для высоких нагрузок
1. Добавьте кеширование через Redis
2. Используйте connection pool для БД
3. Разделите бота на микросервисы
4. Настройте балансировку нагрузки

### Мониторинг
1. Интегрируйте с Prometheus для метрик
2. Настройте алерты через Grafana
3. Используйте Sentry для ошибок
4. Мониторинг через UptimeRobot

## 🤝 Вклад в проект

### Правила разработки
1. Следуйте Git Flow
2. Пишите тесты для нового кода
3. Документируйте изменения
4. Используйте black для форматирования

### Процесс внесения изменений
```bash
# 1. Создайте ветку для фичи
git flow feature start your-feature

# 2. Внесите изменения и напишите тесты
# 3. Проверьте код
pytest
black src/

# 4. Завершите фичу
git flow feature finish your-feature

# 5. Создайте pull request
```

### Стиль кода
- Форматирование: Black
- Сортировка импортов: isort
- Проверка типов: mypy (опционально)
- Линтинг: pylint

## 📄 Лицензия

Этот проект лицензируется под MIT License - смотрите файл [LICENSE](LICENSE) для деталей.


## 🙏 Благодарности

- [Aiogram](https://docs.aiogram.dev/) - Асинхронный фреймворк для Telegram ботов
- [SQLAlchemy](https://www.sqlalchemy.org/) - ORM для Python
- [Docker](https://www.docker.com/) - Контейнеризация приложений

---

**Примечание**: Этот проект предназначен для образовательных целей. Убедитесь в соблюдении законодательства вашей страны при использовании платежных систем.

---
*Последнее обновление: 2025*